"use client"
// import { usePathname } from "next/navigation"
import LeftSideBar from "./component/LeftSideBar"

function LeftSideBarWrapper() {
  // const pathname = usePathname()
  // console.log(pathname, "LeftSideBarWrapper")
  // if (!(pathname === "/deshboard" || pathname === "/login" || pathname === "/signup")) {
    return <LeftSideBar />
  }
  
  
  export default LeftSideBarWrapper